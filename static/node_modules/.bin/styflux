#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../styflux/bin/styflux" "$@"
  ret=$?
else 
  node  "$basedir/../styflux/bin/styflux" "$@"
  ret=$?
fi
exit $ret
